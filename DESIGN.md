# DESIGN.md — terra-town

> 全プロジェクト共通基準 `C:\Users\moets\.claude\DESIGN_STANDARDS.md` に準拠し、terra-town 固有の決定を記録する。
> ゲート②（plan.md）と同時作成。実装は本書のトークン・ルールに従い、カラーコード・サイズの直書きをしない。
> 逸脱が必要な場合は「プロジェクト固有ルール」に理由付きで追記してから実装する。

## 基本方針
- **対象プラットフォーム**: Android 先行 / 将来 iOS
- **準拠ガイドライン**: Material Design 3（Android）＋ HIG（将来 iOS）。アクセシビリティは WCAG 2.1 AA 相当
- **使用コンポーネントライブラリ**: Flutter 標準 Material 3（`useMaterial3: true`）＋ 独自デザイントークン（#17 代表回答）
- **地図SDK**: MapLibre GL Native（地図スタイルは別途 style.json で管理）
- **トーン&マナー**: 「実世界を自分の街に育てる」温かみ・探索のワクワク。ただし**操作系（ボタン・状態表示・エラー）は Material 3 準拠**（世界観優先でも操作系は基準に従う）
- **テーマ**: **地図はライトのみ MVP**（ダーク対応は将来 #17）。地図以外の UI（設定・図鑑等）はダークトークンも定義し初期設計に含める（後付けしない）

## アートディレクション（世界観・画づくり）

> 2026-08-04 代表が参照イメージを提示。
> 参照: シドマイヤーズ シヴィライゼーション VII（Firaxis Games）の画づくり
> — [ファミ通の記事](https://www.famitsu.com/article/202408/14935)
> **参照は方向性の共有のみ。素材・意匠を模倣しない。**

### 目指す画（方向性）

**「実世界の地図の上に置かれた、ミニチュアのジオラマ」**。俯瞰でヘクスタイルが並び、
タイルの上に小さな建物が乗っていて、歩くほどジオラマが広がっていく感覚を狙う。

### 参照作品と terra-town の既存決定の一致（なぜこの参照が有効か）

| 参照側の要素 | terra-town の既存決定 |
|-------------|---------------------|
| ヘクス（六角形）タイルのマップ | `docs/terrain.md` §3・§4「表示はヘクス約50m・内部判定は矩形グリッド」 |
| タイルの属性で建てられるものが決まる | `docs/buildings.md` §3「空き地のみ建築可」「リゾート＝海隣接／ミュージアム＝市街隣接」 |
| 都市と郊外の概念 | `docs/terrain.md` §2 の地形タイプ（市街／農地／空き地）＋ 自治体区画（#7） |
| ジオラマ感（建物が細かく動く） | `spec.md` §4「資材で建物を建設・強化 → 街が育つ」の情緒 |

→ **参照は既存決定と矛盾しない。** 新たな仕様変更は発生せず、画づくりの方向づけとして採用する。

### MVP のフィデリティ（重要・代表指示）

**MVP では参照作品ほどの解像度は不要**（2026-08-04 代表）。狙いは「方向性の担保」であって作り込みではない。

| 段階 | 建物の表現 | 備考 |
|------|-----------|------|
| **MVP** | **平面〜2.5D のスプライト**（斜め見下ろしのイラスト）をヘクス中心に配置 | シンボルレイヤーで実現可。低コスト |
| 将来 | 立体表現（押し出し／簡易モデル）・煙などの微アニメーション | 性能と作り込みコストを見てから |

- MVP では**アニメーションを持たせない**。「静かなジオラマ」で十分に方向性は出る。
- 建物の見た目は種別が識別できることを最優先する（住宅／マンション／畑／農場／工場／採石場／リゾート／ミュージアムの8種・`docs/buildings.md` §2。採石場はIssue #72で追加された8種目であり、7種と書いていた旧記述はIssue #193で誤りを是正した）。
- **建物の正式な絵（2.5Dスプライト）はまだ無い（Issue #193）。** MVPでは Material Icons のグリフ（`app/lib/features/build/build_screen.dart` の `buildingSpecs` と地図の仮アイコンで共通のグリフを使う）＋3系統ごとの既存トークン色の円で仮アイコンとして表示し、正式なスプライトは別途 `IMAGE_WORKFLOW`（承認手順込み）で作成して差し替える。差し替え先は `packages/location/lib/src/map/building_layer.dart` の `buildingIconId`（種別→画像名の対応を1箇所にまとめてある）。

### 技術的含意（plan.md との関係・要注意）

`plan.md` §1-B が採用した **MapLibre GL Native は「地図レンダラ」であってゲームエンジンではない**。
ジオラマ感を出す手段は、実装コストの低い順に次のとおり。**MVP は (1)＋(2) のみとする。**

1. **カメラの pitch（傾き）を付けた俯瞰** — ほぼ無料でミニチュア感が出る。最優先。
2. **建物＝スプライトのシンボルレイヤー** — MVP の建物表現。
3. 簡易な立体（押し出し）— 将来。
4. 3D モデルのカスタムレイヤー — MVP 外。工数が跳ね上がる。

### 🔴 地図スタイル（style.json）はアセットとして扱う

**本節で最も重要な決定。** terra-town が表示するのは実在の地図であり、素の OSM 見た目のままでは
「ジオラマの上に乗ったゲームオブジェクト」と地図が馴染まず、参照作品のような一体感は出ない。

- **MapLibre の `style.json` を技術設定ではなく「アートアセット」として扱う。**
  彩度を落とし、イラスト寄りに寄せて、建物スプライトが浮かないベースにする。
- 配色は本書のカラートークンから外れないこと（地図の緑＝`primary` 系、水面＝`secondary` 系に寄せる）。
- 地図はライトのみ MVP（下記「プロジェクト固有ルール」）なので、style は 1 種類でよい。

### fog of war の見え方

- 現行トークン `fog` はフラットな暗幕。ジオラマ方向に寄せる場合、将来的に「雲・霞」寄りの表現も検討余地がある。
- ただし **MVP はフラットな暗幕のまま**とする。`plan.md` §8 の性能基準（開示1ヘクス追加 200ms 以内・1万ヘクスで 55fps 以上）を満たすことを優先する。
- **地図レイヤーの色は `app` から注入する。`location` は配色を知らない**（Issue #57・注入方式。`packages/location` の `FogOfWarLayer` 等はコンストラクタで16進文字列・αを受け取るだけで、本書のトークンや `Color` 型には依存しない）。

### 開示済みヘクスの土地の見え方（地形タイプ別の色分け）

- 開示済みヘクスは、地形タイプごとに**既存トークンの色を薄く重ねる**（MVP。画像アセットは使わない）。未開示ヘクスは従来どおり `fog` の暗幕のみ。
- **空き地は塗らない**（地図そのまま）。空き地は地域の過半（例: 狭山湖パックで64%）を占めるため、着色すると地図全体が一色に見え、将来の建物スプライトの土台として読みにくくなる。

| 地形タイプ | 塗り色（トークン） | 塗りの不透明度 |
|-----------|------------------|--------------|
| 空き地 | なし | 0 |
| 森 | `primary`（#2E7D32） | 0.22 |
| 山 | `text-secondary`（#4A4B45） | 0.22 |
| 水辺（川・湖） | `secondary`（#00695C） | 0.22 |
| 海 | `info`（#1565C0） | 0.22 |

- **縁取り**: 開示済みヘクス（空き地を含む全地形）に `text-secondary` の線（幅1px・不透明度0.30）を引き、「ヘクスのタイルが並ぶ」ジオラマ感を出す。
- 新しい色値は追加しない（既存トークンの流用）。不透明度・線幅は実機確認で微調整してよい（確定値は実装で微調整・plan §16 と同じ扱い）。
- **色だけで情報を伝えない**（本書アクセシビリティ規則）: 地形タイプ名は、ヘクスをタップしたときのシートに文字で表示する（既存）。地図上の凡例は MVP では設けない。
- 色は `app` から注入し、`location` は配色を知らない（Issue #57 の注入方式に従う）。

### 建設の流れ（Issue #192・T089）

DESIGN.md「画面一覧と状態」の建設行（目的=開示ヘクス〔空き地〕に建物を建てる／
主要アクション=建物を建設／空=建設可能地なし／エラー=資材不足を明示）を、
次の3段階の流れで実現する。

1. **建設タブ**（`app/lib/features/build/build_screen.dart`）
   - 8種の建物（`docs/buildings.md` §2）をカードで並べ、それぞれに建設コスト
     （木・石・鉄）と、所持資材で足りるかを表示する。
   - 資材が足りない建物は押せない表示にし、不足している資材と量を文字で示す
     （エラー=資材不足を明示。色だけで判断させない）。
   - 今までどおり所持資材の一覧（`InventoryScreen`・Issue #150）も下半分に残す。
   - 建物を選ぶと、地図タブに切り替わり「地図で建てる場所を選ぶ」状態になる
     （`app/lib/features/build/build_selection_controller.dart` の
     `BuildSelectionController` がタブ横断でこの選択状態を持つ）。
2. **地図で場所を選ぶ**（`app/lib/features/map/map_screen.dart`）
   - 選んだ建物を建てられるマス（開示済みの空き地。リゾート・ミュージアムは
     隣接条件も満たすマス）を、`success` トークンを薄く重ねてハイライトする
     （不透明度0.35。地形タイプ別色分け〔本書「開示済みヘクスの土地の見え方」・
     不透明度0.22〕より濃くし、「今選べる操作対象」であることを常時表示の
     地形色より目立たせる）。色は `app` から注入し、`location` は配色を知らない
     （Issue #57 の注入方式。`app/lib/map/buildable_highlight_layer_factory.dart`）。
   - 画面上部（`WalkStatsHud` の直下）に「『（建物名）』を建てる場所を選んで
     ください」というバナーを表示し、「やめる」で選択状態を終える。新しい
     固定位置UI要素は増やさない（既存の複数の不具合〔#137/#138/#141/#142/#151〕
     の再発防止のため、新設バナーは既存のHUDと同じ領域に積む）。
   - 建てられるマスが1つも無い場合は、同じバナーに「建てられる場所がありません
     （開示済みの空き地がない／条件を満たす空き地がない等）」を表示する
     （空=建設可能地なし）。
   - ハイライトの有無にかかわらずマスをタップすると確認シート
     （`app/lib/features/map/widgets/build_confirm_sheet.dart` の
     `BuildConfirmSheet`）を開く。建てられない場合は理由を文字で示す。
   - **選択中は霧のヘクスのタップ（開放ポイントのシート・`HexOpeningSheet`）より
     建設のタップ処理を優先する**（同時に両方は開かない。選択が無い間は従来どおり
     開放ポイントのシートを開く）。
3. **建てた後**
   - 成功を知らせる（SnackBar・`success` トークン）。
   - 所持資材の表示（建設タブ）は `InventoryScreen`/`BuildScreen` 自身の定期
     ポーリングで更新される（新たな配線は追加しない）。

新しい色値は追加しない（既存の `success` トークンの流用。本節「地図で場所を
選ぶ」の不透明度は実機確認で微調整してよい。開示済みヘクスの土地の見え方と
同じ扱い）。

### 建物レイヤーの表示（Issue #193・T090）

建てた建物は、ヘクスの中心に1つずつシンボル（仮アイコン）で表示する
（`packages/location/lib/src/map/building_layer.dart` の `BuildingLayerController`）。

- **重なり順**: ベース地図 → 地形タイプ別色分け（塗り→縁取り） → **建物** →
  fog → 名所ピン → 現在地マーカー。地形の色分けより上に見えることを優先し、
  万一同じヘクスに名所と建物が重なった場合は名所ピンが上に見えるようにする
  （地図タップの優先順位〔建設モード中 > 開放ポイント〕とは独立した、描画順のみの決定）。
- **仮アイコン**: 建設タブのカード（`app/lib/features/build/build_screen.dart`
  の `buildingSpecs`）と同じ Material Icons のグリフを、3系統ごとの既存
  トークン色の円の上に描く（`app/lib/map/building_layer_factory.dart`）。
  - 住宅系（住宅・マンション）: `primary`
  - 生産系（畑・農場・工場・採石場）: `secondary`
  - 娯楽系（リゾート・ミュージアム）: `accent`
  名所ピンの背景色（開示済み=`primary`・収集済み=`accent`）と同じトークンを
  再利用する組み合わせがあるが、アイコングリフが異なるため「色だけで情報を
  伝えない」（本書アクセシビリティ規則）は満たす。新しい色値は追加しない。
- **再起動なしの反映**: 建築成功のたびに `building` テーブルを読み直して
  GeoJSON ソースを丸ごと差し替える（`BuildingLayerController.refresh`）。
- 建物の正式な絵（2.5Dスプライト）は本Issueの対象外。上記「MVPのフィデリティ」
  の追記のとおり別途 `IMAGE_WORKFLOW` で作成する。

### 申し送り

- **[#24](https://github.com/rokusoudo-product/terra-town/issues/24)（R1・R2 スパイク）の検証項目に「スプライトのシンボルレイヤーで建物を配置できるか」「カメラ pitch を付けた状態での性能」を追加すること。**
  現在の検証項目はローカル MBTiles 読込・動的レイヤ操作・feature-state・fog 性能が中心で、
  本節で決めた建物表現の実現性が検証範囲に入っていない。

## カラートークン
> 60-30-10 ルール（ベース60/サブ30/アクセント10）。アクセントは1色。実装時に Material 3 の ColorScheme へマッピング。確定値は実装で微調整（plan §16）。

| トークン | ライト | ダーク | 用途 |
|---------|-------|-------|------|
| primary | #2E7D32 | #7CC47F | 主要アクション・自分の街の象徴色（探索の緑） |
| secondary | #00695C | #4DB6AC | サブアクション・区画/水辺系 |
| accent | #F9A825 | #FFCA45 | アクセント（獲得・名所ハイライト、10%） |
| background | #FAFAF7 | #121410 | 画面背景（地図以外） |
| surface | #FFFFFF | #1E211C | カード・パネル・HUD 背景 |
| text-primary | #1B1C18 | #E4E4DC | 本文 |
| text-secondary | #4A4B45 | #A8AAA0 | 補助テキスト |
| success | #2E7D32 | #7CC47F | 建設成功・制覇達成 |
| warning | #F9A825 | #FFCA45 | 注意（電池・権限） |
| error | #C62828 | #EF9A9A | エラー・偽装検出時の通知 |
| info | #1565C0 | #90CAF9 | 情報・チュートリアル |
| fog | rgba(20,22,16,0.62) | — | fog of war 暗幕（未開示ヘクス）。地図はライトのみのため単一 |

- 色だけで情報を伝えない（アイコン＋テキスト併用）。地図上の資材/名所はアイコン＋ラベル。
- コントラスト比 WCAG AA 以上（通常 4.5:1）。

## タイポグラフィ
- **フォント**: 日本語 UI = Noto Sans JP（モバイルはシステムデフォルト可）。
- **スケール**: 12 / 14 / 16 / 20 / 24 / 32（sp）。本文 16、補助 12。行間 1.5〜1.7。
- 地図オーバーレイのラベルのみ視認性のため縁取り可（プロジェクト固有ルール参照）。

## 余白・レイアウト
- **8pt グリッド**（8/16/24/32/48/64、例外は 4 のみ）。
- **基本構造**: 全画面地図をベースに、上部に軽量ステータス（資材・現在区画）、下部にナビ（地図 / 建設 / 図鑑 / 設定）。主要アクションは1画面1つ。
- タップ対象 最低 48×48dp。

## 画面一覧と状態
| 画面 | 目的 | 主要アクション | 4状態の特記 |
|------|------|--------------|------------|
| マップ（ホーム） | 実地図＋fog＋自分の街を見る | （歩く＝GPS） | ローディング=地域パック読込／空=未開示（霧のみ）／エラー=GPS取得不可・権限拒否 |
| 建設 | 開示ヘクス（空き地）に建物を建てる | 建物を建設 | 空=建設可能地なし／エラー=資材不足を明示 |
| 図鑑（名所コレクション） | 集めた名所を見る（#6/#12統合） | 名所を確認 | 空=未収集の見せ方（Materialアイコン＋「？」）／ローディング |
| 区画 | 制覇率・発展度 | 区画を選ぶ | 空=未進出区画 |
| 設定 | プライバシーゾーン・権限・エクスポート/インポート | ゾーン設定・データ書出/読込 | エラー=インポート失敗時の復旧案内 |

- 各画面に **通常/ローディング/空/エラー** の4状態を必ず実装。
- ユーザー操作に 200ms 以内でフィードバック。
- **破壊的操作（インポートによる上書き・データ削除）は確認ステップまたは Undo 必須**。

## アセット

> 生成フローの正本は `C:\Users\moets\.claude\IMAGE_WORKFLOW.md`。
> **キーワードから画像を作らない。** spec / DESIGN.md を読み、代表の承認を得てから確定する。

| アセットID | 用途 | 寸法/比率 | 生成手段 | パス | 状態 |
|-----------|------|----------|---------|------|------|
| `app-icon` | ストア掲載・legacy ランチャー | 1:1 / 512px | **SVG**（Claude 記述） | `assets/app-icon.svg` | 確定（2026-08-01 代表承認） |
| `app-icon-background` | Adaptive Icon 背景レイヤー | 1:1 / 512px | **SVG** | `assets/app-icon-background.svg` | 同上 |
| `app-icon-foreground` | Adaptive Icon 前景レイヤー | 1:1 / 512px | **SVG** | `assets/app-icon-foreground.svg` | 同上 |

### app-icon の構図と根拠

立体的なヘクスタイル5枚。中央＝自分の街、左上＝森、左下＝山、右上＝霧（未開示）、
右下＝霧が半分だけ晴れた状態で、その上に人が霧の側へ歩いている。街から人へ黄の点線（移動の軌跡）。

| 絵の要素 | 根拠 |
|---------|------|
| 六角形タイル | `docs/terrain.md` §4「UI表示のみヘクスとして描画する」 |
| 中央の街・建物 | `specs/001-mvp/spec.md` §4 コアループ「資材で建物を建設・強化 → 街が育つ」 |
| 森 / 山 | `docs/terrain.md` §2 地形タイプ（森=木、山=石・鉄） |
| 暗幕（右側） | 本書の `fog` トークン ／ spec.md §4「通った場所の霧が晴れ実地図が開示される」 |
| 右下の半分だけの暗幕 | 本書「開示で穴を開ける表現」。開拓が進行中であることを示す |
| 歩く人・黄の点線 | spec.md §4「歩く（GPS で移動を記録）」 |

- 配色は本書のカラートークンのみ。SVG 内に `<!-- token: primary -->` 形式で対応を記載している
- 立体（厚み）表現は「タイルが何を描いているか分かりにくい」という代表フィードバック（2026-08-01）への対応

### Adaptive Icon の制約

- **すべての要素を中央 66dp（本 SVG の座標で中心 (256,256) 半径 156.4）の内側に収めること。**
  ランチャーが円・しずく・角丸・ティアドロップなど任意のマスクを適用するため、外側は切られる
- タイル群の外接範囲は x 144〜368 / y 162.2〜366.8（中心からの最大距離 146）
- **背景レイヤーに角丸を付けない。** マスクは OS が適用するため、二重に丸まる
- 合成版 `app-icon.svg` も角丸なし（Play ストアが角丸を適用する）
- 3ファイルは内容が重複している。**修正時は3つとも更新すること**（前景の定義が正）

### PNG 化（[#28](https://github.com/rokusoudo-product/terra-town/issues/28) で整備済み）

SVG が正本。Android の mipmap と Play 掲載画像には PNG が必要。

- **変換手段**: cairosvg（Python）。`<use>` / `<defs>` / `clipPath` を正しく解釈できるため採用（本アセットは `<use>` を多用）。
  rsvg-convert / Inkscape / ImageMagick は不採用
- **導入先**: リポジトリ専用の venv（`tools/.venv`。`.gitignore` 済み）。導入手順は `docs/dev-setup.md` §8
- **変換スクリプト**: `tools/rasterize_assets.py`。`assets/*.svg` を mipmap 5段階（mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi）
  + Play 掲載用 512px の PNG に変換する（出力先はデフォルト `build/rasterized/`、再生成可能なため未コミット）
- `assets/app-icon.svg` → 512×512 PNG で立体表現・霧の半透明・歩く人の欠けがないことを確認済み
- 現状の `app/android/app/src/main/res/mipmap-*/ic_launcher.png` は Flutter のデフォルトのまま（未差し替え）。
  実機への反映（mipmap 差し替え・`mipmap-anydpi-v26/ic_launcher.xml` の追加・`flutter_launcher_icons` の導入）は
  別Issue（[#29](https://github.com/rokusoudo-product/terra-town/issues/29)）の範囲

## プロジェクト固有ルール
- **対応ロケールは日本語（`ja`）のみ**。UI 文言はコード内に日本語で直書きし、多言語化（ARB/`intl` 導入）は MVP 外とする（[#51](https://github.com/rokusoudo-product/terra-town/issues/51)）。
- **地図はライトテーマのみ MVP**（ダークは将来）。地図以外の UI はライト/ダーク両対応。
- 地図オーバーレイのテキストは視認性のため縁取り（ハロー）を許可＝DESIGN_STANDARDS の「直書き禁止」の例外として、地図ラベル専用トークンを用いる。
- fog of war の暗幕はトークン `fog` を用い、全ヘクスをソースに追加した上で `fill-opacity` を feature-state（`revealed`）で切り替える表現とする（未開示=`fog` トークンの不透明度・開示=0。plan §8）。
- HUD などゲーム世界観のパネルは独自トーンを許可するが、ボタン・トグル・エラー表示は Material 3 コンポーネントを用いる。
- 偽装検出時のユーザー通知は「罰しない」トーン（error 色は使うが文言は非難的でなく「この区間は報酬対象外です」）。
- **初回資材付与（チュートリアル）の通知**（[Issue #184](https://github.com/rokusoudo-product/terra-town/issues/184)）: 初回起動時に開始時の資材（`spec.md` §7.1・`docs/tutorial.md`）を付与したことを、Material 3 の `SnackBar` で知らせる（例:「はじめの資材として 木50・石10 を受け取りました」）。色はカラートークン表の `info`（用途「情報・チュートリアル」）を用いる。破壊的操作ではないため確認ステップ・Undo は不要（本書「画面一覧と状態」の破壊的操作規則の対象外）。
