/// ヘクス幾何に関する定数。
///
/// GPS_ARCHITECTURE 準拠: 本ファイルは数値を保持するだけであり、
/// 緯度経度からヘクス幾何への変換ロジックは持たない（変換は `location/` の責務。
/// docs/terrain.md §4・2026-08-11 代表回答）。
library;

/// 1ヘクスの対辺（辺と向かい合う辺の距離）〔メートル〕。
///
/// 出典: `docs/terrain.md` §3
/// 「1ヘクスの対辺（辺と向かい合う辺の距離）は約50mとする（仮置き、プロトタイプで調整）」。
/// 徒歩数十歩〜1分未満で隣接ヘクスへ移動できる粒度感を狙った初期値であり、
/// balance 検討（plan/tasks 工程）で最終調整される。変更する場合は
/// `docs/terrain.md` §3 を更新したうえで、この定数もあわせて更新すること。
const double hexFlatToFlatMeters = 50.0;
