[pytest]
# tools/pack-builder/ 直下を sys.path に足す（terrain_rules 等を `import terrain_rules`
# のトップレベルモジュールとして import できるようにする。classify_terrain.py 等の
# 本番コードも同じ相対import前提のため揃える）。
pythonpath = .
testpaths = tests
