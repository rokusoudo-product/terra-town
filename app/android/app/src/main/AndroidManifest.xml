<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Issue #123 (T046〜T048)。plan.md §10・Issue #10 の代表回答により
         フォアグラウンド位置のみを要求する。ACCESS_BACKGROUND_LOCATION は要求しない
         （background location 権限のストア審査を回避する設計判断・plan.md §10）。 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Android 14 (API 34) 以降、foregroundServiceType="location" の foreground service を
         開始するには本権限の宣言が必要（マニフェスト宣言のみで、実行時許可ダイアログの対象外）。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Android 13 (API 33) 以降、常駐通知の表示には実行時許可が必要。許可されない場合も
         foreground service 自体は起動する（通知が表示されないだけ）。実行時のリクエストは
         T059（権限リクエストUI）のスコープ。 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Issue #126 (T101)。歩数センサー（TYPE_STEP_COUNTER）による歩数突合に使う。
         API 29 (Android 10) 以降の実行時許可。無い場合は歩数センサーを使わず
         step_count は null のまま（罰しない側に倒す設計・LocationTrackingService
         のドキュメント参照）。API 29 未満はこの権限自体が存在せず常に利用可能。
         Google Play のデータセーフティ申告への影響は docs/location-track-db.md §9
         を参照。 -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>

    <application
        android:label="terra_town"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Issue #123 (T046〜T048): 位置記録 foreground service。
             exported=false（他アプリから起動不可。app 内の composition root からのみ起動する）。
             権限が付与されていない場合は LocationTrackingService#onStartCommand が
             自ら stopSelf() する（サービス自体は「起動できてしまう」が、記録・通知は開始しない）。 -->
        <service
            android:name=".location.LocationTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
